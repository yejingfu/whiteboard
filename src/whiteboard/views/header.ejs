<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/static/libs/bootstrap3.2.0/css/bootstrap.css' />
    <link ref='stylesheet' href='/static/libs/toolbar/jquery.toolbars.css' />
    <link rel='stylesheet' href='/static/css/style.css' />
    <link rel="stylesheet" href="/static/css/toolbar.css">
    <link rel="stylesheet" href="/static/libs/colorpicker/css/colorpicker.css">
    <link rel="stylesheet" href="/static/libs/colorpicker/css/layout.css">
  </head>
  <body>
    <!-- header (banner) -->
    <div class="navbar navbar-inverse navbar-fixed-top" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="/"><%= title %> (POC)</a>
        </div>
        <div class="navbar-collapse collapse">
          <% if (userId) { %>
          <a class="navbar-brand navbar-right" href="#"><%= userId %></a>
          <% } else { %>
          <form class="navbar-form navbar-right" role="form" method="post" action="/signin">
            <div class="form-group">
              <input name="email" type="text" placeholder="Email" class="form-control">
            </div>
            <div class="form-group">
              <input name="passwd" type="password" placeholder="Password" class="form-control">
            </div>
            <button type="submit" class="btn btn-success">Sign in</button>
            <button type="button" class="btn btn-link">Sign up</button>
          </form>
          <% } %>
        </div><!--/.navbar-collapse -->
      </div>
    </div>